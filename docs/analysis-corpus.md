# Analysis Corpus Manifest

Benchmark test corpus for the experimental audio-analysis feature (see
`docs/experimental-audio-analysis.md`). Generated by
`scripts/make-analysis-corpus.sh` (idempotent, re-run any time) into:

```
/Users/Nigel/Developer/Takes/Private/Analysis Corpus/
```

`Private/` is gitignored and absent from worktrees, so the corpus itself is
never committed -- only the generator script and this manifest are. Re-running
the script with no arguments regenerates anything missing; pass `--force` to
regenerate everything, or a path argument to write elsewhere.

Spectrogram PNGs for every file live in `_spectrograms/` (same basename).
Raw verification numbers (re-measured on every run) live in
`_verification.tsv` in the corpus dir.

All measurements below were produced by the script's verification pass:
- Integrated LUFS / true peak: `ffmpeg -af loudnorm=print_format=summary` first-pass ("Input") measurement.
- HF RMS: `ffmpeg -af "highpass=f=17000,astats=measure_overall=RMS_level"`, RMS in dBFS of everything above 17 kHz.
- Empirical cutoff points below were additionally confirmed with a 14-20 kHz highpass sweep and by eye on the spectrograms.

## Sources

### `reference.wav` (synthetic, primary)

30 s / 44.1 kHz / stereo / 24-bit. Built entirely in `ffmpeg` filtergraphs
(`anoisesrc`, `aevalsrc`, `amix`, `alimiter`), fully reproducible, no external
input. Contents, mixed together:

- Two independent pink-noise beds (L/R, different seeds so the stereo image is decorrelated), amplitude 0.12.
- Two harmonic-stack tones standing in for "melodic" content (summed sine partials at 110/220/330/440 Hz and 164.81/329.63/494.44 Hz).
- A percussive burst train: white noise gated by `exp(-35 * (t mod 1.7))`, i.e. a fast-decay hit repeating every 1.7 s.
- A slow linear chirp sweep from 200 Hz to 21 kHz across the full 30 s (`aevalsrc` with the proper quadratic phase term `sin(2*pi*(f0*t + k/2*t^2))`, k=(f1-f0)/duration) -- this gives the corpus genuine full-bandwidth content right up to the cutoff-detector's operating range.
- A ~2.5 s near-silence gap at t = 19.0-21.5 s (gain dropped to 0.02x) for noise-floor ground truth.
- Everything summed then passed through `alimiter` with a linear ceiling of 0.708 (-3 dBFS).

Ground truth: true peak measured at -2.1 dBTP (target was -3 dBFS; close enough -- the limiter's soft knee left a little headroom). Integrated loudness is about -13.6 LUFS. Full energy present to ~21 kHz (no encoder has ever touched this signal).

### `real_reference.wav` (real music, secondary)

30 s window (t = 20-50 s, skipping the intro) decoded from
`Private/Audio Samples/11 Where to Begin.m4a`, resampled/remuxed to
44.1 kHz / stereo / 24-bit PCM. This source is already AAC-sourced -- the
original file is a ~283 kbps AAC encode (confirmed via `afinfo`), so it carries
whatever high-frequency character that encoder left behind (soft rolloff
approaching ~19-20 kHz is visible on its spectrogram, not a hard shelf).
Treat `real_reference.wav` as "known-good-ish, not textbook-lossless" -- it's
here for realism (real transients, real mix, real stereo image), not as a
pristine ground truth like the synthetic reference. Measured: -12.3 LUFS,
true peak -0.0 dBTP.

## Variants

Unless noted, recipes apply to `reference.wav`; `real_*` variants apply the
same recipe to `real_reference.wav`. Filter chains are single ffmpeg
invocations (see the script for exact syntax).

| File | Recipe | Ground truth |
| --- | --- | --- |
| `true_lossless.flac` | `flac --best` of `reference.wav` | Not a transcode -- genuine lossless, full bandwidth. Reads identically to `reference.wav` on every metric (same LUFS/HF numbers by construction). |
| `loud.wav` / `real_loud.wav` | `volume=8dB` then `alimiter` (ceiling ~-0.5 dBFS) | Higher LUFS, reduced crest factor / more limiting than reference. |
| `quiet.wav` | `volume=-8dB`, no limiting | Lower LUFS, same crest factor as reference (pure gain change). |
| `tilt_bassy.wav` | `bass=g=6:f=120` (low shelf, +6 dB @ 120 Hz) | Bass-heavy tilt. HF band unchanged vs reference (shelf doesn't touch highs) -- confirmed below. |
| `tilt_bright.wav` | `treble=g=6:f=8000` (high shelf, +6 dB @ 8 kHz) | Bright tilt. HF band clearly elevated vs reference. |
| `muffled.wav` / `real_muffled.wav` | `lowpass=f=4500,lowpass=f=4500` (two cascaded 2-pole biquads, ~24 dB/oct slope, -3 dB point at 4.5 kHz) | Clearly dull. HF RMS ~40 dB below reference. |
| `hiss.wav` / `real_hiss.wav` | Mix in `anoisesrc=color=white:amplitude=0.00316` (about -50 dBFS) across the whole file, including the "silent" gap | Constant broadband hiss floor, tape/vinyl-style; audible even during the reference's near-silence window. |
| `mp3_128.mp3` / `real_mp3_128.mp3` | `lame -b 128` (CBR) | Lossy, empirical cutoff about 16 kHz (see cutoff table below). |
| `mp3_320.mp3` | `lame -b 320` (CBR) | Lossy but near-transparent; cutoff about 20 kHz, close to reference. |
| `aac_96.m4a` | `ffmpeg -c:a aac_at -b:a 96k` (AudioToolbox AAC) | Lossy, cutoff noticeably lower than 128 kbps MP3 (worse HF retention at this bitrate). |
| `aac_256.m4a` | `ffmpeg -c:a aac_at -b:a 256k` | Lossy but near-transparent; HF numbers close to reference. |
| `fake_lossless_mp3128.flac` | Decode `mp3_128.mp3` -> WAV -> `flac --best` | Lossy transcode wearing a lossless container. Bit-for-bit downstream of the MP3, so it has the exact same ~16 kHz cutoff and HF numbers as `mp3_128.mp3` despite the `.flac` extension. This is the "fake lossless" trap case. |
| `fake_lossless_aac128.flac` | Encode reference -> AAC 128k -> decode -> `flac --best` | Same trap, AAC-128 heritage instead of MP3. |
| `real_fake_lossless_mp3128.flac` | Decode `real_mp3_128.mp3` -> WAV -> `flac --best` | Same trap on the real-music source. |

Not generated as separate files (per spec, only where marked `real_` above):
bassy/bright/quiet/mp3_320/aac tilt-and-rate variants were only produced for
the synthetic reference, since validating those detectors doesn't need a
second real-music instance -- the real-music set focuses on loud/muffled/hiss
and the fake-lossless trap for realism.

## Phase 2: provenance-detection cases

Nine additional files targeting analog-source detection (hiss/clicks/crackle/
rumble, and whether hiss is stereo-decorrelated or a mono duplicate) and
lossy-encode-quality contrasts (encoder-era LAME-tag presence, intensity-
stereo joint coding, and pre-echo around sharp transients). Generated by the
same script; see `build_real_vinyl_sim`, `build_real_tape_sim`,
`build_mp3_192_modern/early/intensity`, and `build_transient_reference` /
`build_transient_variants` in `scripts/make-analysis-corpus.sh`. Small Python
helper scripts (`gen_clicks.py`, `gen_crackle.py`, `gen_transients.py`) are
written by the shell script into `_gen/` inside the corpus dir on every run
(not committed -- regenerated, kept only for inspectability).

A second verification pass (`verify_phase2`) writes
`_verification_phase2.tsv` alongside the phase-1 `_verification.tsv`, with
decorrelation, side-channel rumble, tag-offset, and cutoff-sweep numbers used
in the tables below.

### Analog-source simulations (gapless)

Applied to `real_reference.wav`, which has no quiet passages -- deliberately,
since a detector that only finds hiss/rumble in a silent window would miss it
entirely on a real recording. Both files add their noise across the *entire*
30 s, no gaps.

| File | Recipe | Ground truth |
| --- | --- | --- |
| `real_tape_sim.wav` | Stereo-**decorrelated** hiss only: two independently seeded `anoisesrc=white` sources (seeds 611/612), one per channel, mixed in at a calibrated gain to hit ≈ -50 dBFS overall RMS. No clicks/crackle/rumble. | Constant, gapless hiss floor whose L and R components are statistically independent -- the "real tape" contrast case for a stereo-coherence detector. |
| `real_vinyl_sim.wav` | All four vinyl artifacts combined, gapless, on `real_reference.wav`: (a) stereo-decorrelated hiss (seeds 511/512, independent `anoisesrc=white`, calibrated to ≈ -55 dBFS overall); (b) clicks: a precomputed sparse impulse WAV from `gen_clicks.py` (irregular timing, min 300 ms apart, 0.5-2 ms wideband spikes with a fast exponential-decay envelope, amplitude 0.5-0.95, mixed in equally to both channels); (c) crackle: dense low-level impulsive noise from `gen_crackle.py` (0.1-0.6 ms micro-impulses, ~120/s, amplitudes weighted toward quiet with occasional louder pops, peak amp 0.15); (d) rumble: brown noise cascaded through two 30 Hz lowpass biquads, calibrated to -35 dBFS RMS pre-upmix, then panned to the SIDE channel only (L=+n, R=-n) via `pan=stereo\|c0=c0\|c1=-1*c0`. | Gapless, multi-artifact vinyl-rip simulation. Click count is randomized per run (seeded, so reproducible) -- see measured count below. Rumble is present *only* in the side channel by construction; the source's own bass content still dominates the mid channel in absolute terms (see note below). |

**Is `real_hiss.wav` (v1) correlated or decorrelated?** Checked directly:
`real_hiss.wav` and `hiss.wav` both add a *single* `anoisesrc` source
duplicated to both channels via `aformat=channel_layouts=stereo` before
mixing -- i.e. **fully correlated (mono) hiss**, not stereo noise. This makes
them the correct contrast case for `real_tape_sim.wav`/`real_vinyl_sim.wav`'s
decorrelated hiss: same general "add broadband noise" recipe, opposite
stereo-coherence ground truth. Confirmed empirically below.

### Lossy-encode-quality cases

| File | Recipe | Ground truth |
| --- | --- | --- |
| `mp3_192_modern.mp3` | `lame -b 192` (CBR), default modern settings, on `reference.wav` | Keeps energy to ~19 kHz; embeds a genuine VBR-tag-frame ("Info" marker, LAME CBR mode) at byte offset 36, well inside the first MPEG frame. |
| `mp3_192_early.mp3` | `lame -b 192 -q 9 --lowpass 16 -t` on `reference.wav`, simulating a cheap/late-90s-style encoder: worst quality preset, hard 16 kHz lowpass, `-t` disables the LAME tag entirely | Hard ~16 kHz cutoff (well below the 192 kbps norm) and **no** LAME/Xing/Info marker anywhere near the file start -- confirmed via direct byte search (see below). This is the file that should trip a "suspiciously old/cheap encoder" heuristic on bitrate + tag absence, not just bitrate. |
| `mp3_192_intensity.mp3` | Intensity-stereo **simulation**: `acrossover=split=8000:order=8th` splits `reference.wav` into low/high bands, the high band is downmixed to mono (`pan=mono\|c0=0.5*c0+0.5*c1`) and fed identically to both channels, recombined with the (still-stereo) low band, then `lame -b 192`. This is a simulation of the joint-stereo-coding *effect*, not LAME's actual mid/side or intensity-stereo internals -- called out explicitly since it's a proxy, not a bit-accurate reproduction of what a real intensity-stereo encoder does. | Above 8 kHz, L and R collapse toward mono (confirmed via mid/side RMS below); below 8 kHz stereo image is unchanged from `reference.wav`. |
| `transient_reference.wav` | 30 s / 44.1 kHz / stereo synthetic percussion via `gen_transients.py`: sharp castanet-like noise bursts (1-5 ms, instant onset, fast exponential decay) on an irregular ~4/s grid, slight per-hit stereo placement, over near-silence. Peaks calibrated to ≈ -6 dBFS. | Pre-echo probe material: isolated sharp transients over silence are exactly the case where MP3's block-based quantization smears energy backward in time before the transient onset. |
| `transient_mp3_128.mp3` | `lame -b 128` of `transient_reference.wav` | Pre-echo clearly present -- visible as a faint noise smear in the ~10-20 ms window before each onset on the spectrogram. |
| `transient_mp3_320.mp3` | `lame -b 320` of `transient_reference.wav` | Contrast case: little to no audible/visible pre-echo at this bitrate. |
| `transient_fake_lossless_mp3128.flac` | Decode `transient_mp3_128.mp3` -> WAV -> `flac --best` | Same fake-lossless trap as the v1 files, but this time the smoking gun is pre-echo rather than a hard HF cutoff -- the pre-echo artifact survives the FLAC re-wrap exactly because FLAC is genuinely lossless downstream of the (already lossy) MP3. |

## Measured results

From `_verification.tsv` (regenerated by the script on every run; numbers
below are from the latest generation pass). "HF RMS (>17 kHz)" is in dBFS --
more negative means less high-frequency energy.

| File | Format | Integrated LUFS | True peak (dBTP) | HF RMS >17 kHz (dB) |
| --- | --- | --- | --- | --- |
| reference.wav | wav, pcm_s24le | -13.6 | -2.1 | -26.8 |
| true_lossless.flac | flac | -13.6 | -2.1 | -26.8 |
| loud.wav | wav, pcm_s24le | -5.6 | +2.8 | -18.9 |
| quiet.wav | wav, pcm_s24le | -21.6 | -10.1 | -34.8 |
| tilt_bassy.wav | wav, pcm_s24le | -13.4 | -2.1 | -26.8 |
| tilt_bright.wav | wav, pcm_s24le | -9.1 | +2.7 | -20.8 |
| muffled.wav | wav, pcm_s24le | -20.8 | -6.8 | -68.9 |
| hiss.wav | wav, pcm_s24le | -19.6 | -8.2 | -32.9 |
| mp3_128.mp3 | mp3 | -14.4 | -2.5 | -36.1 |
| mp3_320.mp3 | mp3 | -13.7 | -2.5 | -28.1 |
| aac_96.m4a | m4a/aac | -15.4 | -1.9 | -39.5 |
| aac_256.m4a | m4a/aac | -13.6 | -2.8 | -26.9 |
| fake_lossless_mp3128.flac | flac | -14.4 | -2.5 | -36.1 |
| fake_lossless_aac128.flac | flac | -14.3 | -2.8 | -34.1 |
| real_reference.wav | wav, pcm_s24le | -12.3 | -0.0 | -53.3 |
| real_loud.wav | wav, pcm_s24le | -7.2 | +0.1 | -48.4 |
| real_muffled.wav | wav, pcm_s24le | -12.5 | -0.2 | -74.8 |
| real_hiss.wav | wav, pcm_s24le | -18.4 | -6.0 | -59.0 |
| real_mp3_128.mp3 | mp3 | -12.8 | -0.2 | -56.5 |
| real_fake_lossless_mp3128.flac | flac | -12.8 | -0.2 | -56.5 |
| real_tape_sim.wav | wav, pcm_s24le | -12.3 | -0.1 | -51.6 |
| real_vinyl_sim.wav | wav, pcm_s24le | -12.3 | +0.1 | -50.1 |
| mp3_192_modern.mp3 | mp3 | -14.1 | -1.1 | -30.6 |
| mp3_192_early.mp3 | mp3 | -15.4 | -2.7 | -38.3 |
| mp3_192_intensity.mp3 | mp3 | -14.1 | -3.6 | -30.7 |
| transient_reference.wav | wav, pcm_s24le | -36.6 | -3.8 | -48.9 |
| transient_mp3_128.mp3 | mp3 | -38.3 | -4.9 | -58.5 |
| transient_mp3_320.mp3 | mp3 | -36.9 | -3.9 | -50.8 |
| transient_fake_lossless_mp3128.flac | flac | -38.3 | -4.9 | -58.5 |

Notes on the numbers:
- `true_lossless.flac` matches `reference.wav` exactly, as expected for a non-lossy container.
- `fake_lossless_mp3128.flac` matches `mp3_128.mp3` exactly (same LUFS, same HF RMS) -- the FLAC wrapper doesn't change the underlying (already-lossy) signal, which is exactly the property the "is this really lossless" detector needs to catch.
- `tilt_bassy.wav`'s HF RMS (-26.8) is indistinguishable from `reference.wav` (-26.8) -- correct, since a 120 Hz low shelf shouldn't move energy above 17 kHz.
- `tilt_bright.wav`'s HF RMS (-20.8) is about 6 dB above reference -- correct, matches the +6 dB high-shelf boost at 8 kHz (shelf gain fully applied by 17 kHz).
- `loud.wav`/`quiet.wav` differ by ~16 LU rather than a clean 16 dB (8+8) because `loud.wav` additionally runs through a limiter that reduces crest factor, which nonlinearly affects the gated LUFS measurement -- expected, not a bug.
- Real-music derivatives sit at consistently higher HF RMS values than their synthetic counterparts because the real recording has less energy above 17 kHz to begin with (it's an AAC-sourced track, already rolled off before we touch it) -- the deltas (muffled vs reference, hiss vs reference) are what matter, not the absolute numbers matching the synthetic set.
- `real_tape_sim.wav` (-51.6) and `real_vinyl_sim.wav` (-50.1) land close to their targeted -50/-55 dBFS hiss levels; `real_vinyl_sim.wav` reads slightly louder than `real_tape_sim.wav` in HF RMS because it's also carrying crackle and click energy in the same band, on top of similar hiss.
- `mp3_192_early.mp3` (-38.3) is markedly lower than `mp3_192_modern.mp3`/`mp3_192_intensity.mp3` (both -30.6/-30.7) in HF RMS above 17 kHz -- consistent with its hard 16 kHz lowpass vs. the ~19 kHz-ish rolloff of a normal 192 kbps encode.
- `transient_mp3_128.mp3` and `transient_fake_lossless_mp3128.flac` report identical numbers across the board (LUFS, true peak, HF RMS) -- expected, since the FLAC is a lossless re-wrap of the MP3 and carries the same pre-echo forward untouched.

## Phase 2 measurements: decorrelation, rumble, tags, intensity-stereo

From `_verification_phase2.tsv` (regenerated alongside `_verification.tsv` on
every run). "Side" = RMS of `0.5*(L-R)`, "mid" = RMS of `0.5*(L+R)`, both in
dBFS; smaller mid-side gap (mid closer to side) means more decorrelated/wider
stereo image in that band.

### Hiss coherence: correlated (v1) vs decorrelated (v2), RMS above 10 kHz

| File | Side RMS (dB) | Mid RMS (dB) | Gap (mid - side) |
| --- | --- | --- | --- |
| real_reference.wav (untouched baseline) | -50.0 | -42.8 | 7.2 |
| real_hiss.wav (v1, mono/correlated hiss) | -56.0 | -48.7 | 7.3 |
| real_tape_sim.wav (v2, decorrelated hiss) | -49.0 | -42.6 | 6.4 |
| real_vinyl_sim.wav (v2, decorrelated + clicks/crackle/rumble) | -49.7 | -42.0 | 7.7 |
| hiss.wav (synthetic, v1, mono/correlated) | -47.6 | -29.4 | 17.8 |

At the >10 kHz band, the gap doesn't cleanly separate correlated from
decorrelated on the real-music files -- `real_reference.wav`'s *own* stereo
image already has real (non-hiss) HF content, and at -50/-55 dBFS the added
hiss is comparable in level to that residual, so it doesn't dominate the
measurement. Re-running the same side/mid comparison restricted to >20 kHz,
where `real_reference.wav`'s own AAC-sourced HF content has mostly rolled
off and the added noise dominates, separates them clearly:

| File | Side RMS >20kHz (dB) | Mid RMS >20kHz (dB) | Gap |
| --- | --- | --- | --- |
| real_reference.wav | -67.3 | -63.0 | 4.3 |
| real_hiss.wav (correlated) | -73.3 | -67.8 | 5.6 (tracks the untouched baseline -- mono hiss doesn't add width) |
| real_tape_sim.wav (decorrelated) | -61.8 | -60.1 | 1.7 (collapses toward 0 -- side and mid converge, the decorrelation signature) |
| real_vinyl_sim.wav (decorrelated + clicks/rumble) | -64.9 | -55.7 | 9.2 (noisier -- clicks/crackle/rumble also contribute non-hiss energy in this range) |

Practical implication for the v2 coherence detector: **don't test raw
full-band mid/side ratio and expect a clean signal on real material** --
isolate the noise floor first (e.g. a high-enough band that the source's own
content is negligible, or a spectral-flatness/noise-floor estimate) before
comparing side vs. mid. The synthetic `hiss.wav` case (bottom row) shows what
a clean, unambiguous correlated-noise signature looks like when there's no
competing real-source HF content to confound it.

The purely synthetic decorrelated-noise construction (no real source mixed
in) was independently confirmed during script development to converge to a
~0.0 dB mid-side gap for two independently seeded noise sources and a
~7-18 dB gap for one mono source duplicated to both channels -- the
generation recipe itself is correct; the ambiguity above is specifically a
real-music/at-this-noise-level measurement artifact, not a bug in
`real_tape_sim.wav`/`real_vinyl_sim.wav`.

### Vinyl rumble: side-channel sub-30 Hz level

| File | Side RMS <30Hz (dB) | Mid RMS <30Hz (dB) |
| --- | --- | --- |
| real_reference.wav (baseline, no rumble) | -45.6 | -31.2 |
| real_vinyl_sim.wav (rumble added, side-only) | -35.2 | -31.2 |

The side channel jumps ~10 dB (-45.6 -> -35.2) once rumble is added, while
the mid channel is unchanged (-31.2 both) -- confirming the rumble is
injected purely as a side-channel (L=+n, R=-n) signal, as specified. Note the
mid channel's absolute <30 Hz level (-31.2 dB) is *louder* than the injected
side-channel rumble (-35.2 dB) in both files -- that's the real recording's
own bass/kick content, which the rumble sim doesn't attempt to swamp; the
diagnostic signal is the side-channel *increase*, not an absolute threshold.

### Vinyl click count (real_vinyl_sim.wav)

Intended/placed click count for the current corpus generation: **16 clicks**
over 30 s (`gen_clicks.py` targets a random rate of 20-40/min per run, seeded
for reproducibility; this run rolled ~31.6/min but the "no closer than 300 ms
apart" rejection rule reduced the achievable count in a 30 s window to 16).
The exact count is written to `_vinyl_click_count.txt` in the corpus dir and
mirrored into `_verification_phase2.tsv` as `intended_click_count` on every
run, so it stays truthful even if the script or seeds change later.

### mp3_192_early vs mp3_192_modern: cutoff sweep and tag presence

| File | >14kHz | >15kHz | >16kHz | >17kHz | >18kHz | >19kHz | >20kHz |
| --- | --- | --- | --- | --- | --- | --- | --- |
| mp3_192_modern.mp3 | -27.3 | -28.1 | -29.2 | -30.6 | -32.8 | -36.5 | -42.8 |
| mp3_192_early.mp3 | -31.7 | -33.4 | -35.6 | -38.3 | -42.0 | -46.9 | -53.9 |
| mp3_192_intensity.mp3 | -27.4 | -28.2 | -29.2 | -30.7 | -32.9 | -36.6 | -42.9 |

`mp3_192_modern.mp3` keeps meaningful energy out to ~19 kHz before the
typical MP3 near-Nyquist rolloff; `mp3_192_early.mp3` is already 4-11 dB
lower at every point in this sweep and drops off starting well before
16 kHz, confirming its forced `--lowpass 16` cutoff. `mp3_192_intensity.mp3`
tracks `mp3_192_modern.mp3` almost exactly in this sweep (as expected -- the
intensity-stereo simulation changes stereo width, not the overall spectral
envelope).

Tag byte-offset search (`file.find(b"LAME"/"Xing"/"Info")`, -1 = not found):

| File | LAME offset | Xing offset | Info offset | File size |
| --- | --- | --- | --- | --- |
| mp3_192_modern.mp3 | 156 | -1 | 36 | 721,605 B |
| mp3_192_early.mp3 | 720,481 | -1 | -1 | 720,979 B |
| mp3_192_intensity.mp3 | 156 | -1 | 36 | 721,605 B |

`mp3_192_modern.mp3`/`mp3_192_intensity.mp3` both carry a genuine LAME
VBR-tag-frame (LAME writes "Info" for CBR/ABR streams, "Xing" for true VBR --
we're CBR here, so "Info" is correct) right after the first MPEG frame
header at byte 36, with the "LAME3.100..." version string embedded inside
that same tag frame at byte 156. `mp3_192_early.mp3`'s only "LAME" match is
at byte 720,481 -- 498 bytes from EOF, and inspection shows it's a
coincidental match inside a run of `0x55` padding bytes in the last frame,
**not** a structured tag (no "Xing"/"Info" match anywhere in the file, and
the first 4 bytes are a plain `0xFFFB` MPEG frame sync with no tag frame
following). This is the intended contrast: `-t` genuinely suppresses the tag
rather than just moving it.

### Intensity-stereo simulation: decorrelation above/below the 8 kHz split

| File | Side >8kHz (dB) | Mid >8kHz (dB) | Side <8kHz (dB) | Mid <8kHz (dB) |
| --- | --- | --- | --- | --- |
| reference.wav (baseline) | -40.9 | -22.6 | n/a (not measured; below-split behavior is verified via the intensity file itself) | n/a |
| mp3_192_intensity.mp3 | -51.5 | -23.9 | -30.8 | -23.4 |

Above 8 kHz, `mp3_192_intensity.mp3`'s side-channel level drops sharply
(-51.5 dB vs. reference's -40.9 dB, ~11 dB more collapsed toward mono) while
the mid level is essentially unchanged (-23.9 vs -22.6) -- the signature of
the high band being downmixed to mono and fed identically to both channels.
Below 8 kHz, side and mid sit much closer together (-30.8 / -23.4, a 7.4 dB
gap vs. the >8kHz band's 27.6 dB gap), confirming the low band keeps its
original stereo width -- exactly the intended "simulation," not a full-band
mono collapse.

## Empirical cutoff confirmation

Highpass sweep (RMS in dBFS above each cutoff) -- used to locate the actual
knee point for the lossy variants, per the spec's verification step:

| File | >14kHz | >15kHz | >16kHz | >17kHz | >18kHz | >19kHz | >20kHz | Empirical cutoff |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| reference.wav | -25.0 | -25.5 | -26.1 | -26.8 | -27.9 | -29.5 | -32.1 | none (full-band; gentle rise is just less broadband noise energy near Nyquist) |
| mp3_128.mp3 | -30.4 | -31.8 | -33.6 | -36.1 | -39.5 | -44.2 | -51.1 | ~16 kHz -- sharp, accelerating rolloff typical of MP3 128 kbps |
| mp3_320.mp3 | -25.8 | -26.4 | -27.1 | -28.1 | -29.5 | -31.9 | -36.4 | ~20 kHz, close to reference -- near-transparent |
| aac_96.m4a | -32.3 | -34.2 | -36.5 | -39.5 | -43.2 | -48.2 | -55.2 | ~15-16 kHz, rolloff steeper than mp3_128 at the same highpass points -- AAC 96 kbps cuts harder than MP3 128 |
| aac_256.m4a | -25.1 | -25.6 | -26.1 | -26.9 | -28.0 | -29.5 | -32.2 | matches reference -- near-transparent |
| muffled.wav | -59.6 | -62.3 | -65.4 | -68.9 | -73.0 | -78.1 | -85.2 | 4.5 kHz by construction (cascaded lowpass), confirmed -- 40+ dB below reference throughout the band |
| real_reference.wav | -47.7 | -49.4 | -51.3 | -53.3 | -55.7 | -58.3 | -61.7 | soft rolloff from ~19 kHz, consistent with its AAC ~283 kbps source |
| real_muffled.wav | -65.4 | -68.2 | -71.3 | -74.8 | -78.9 | -84.0 | -91.1 | 4.5 kHz, same recipe as synthetic muffled |

This confirms the spec's key discriminator empirically: `mp3_128`-derived
files (including the fake-lossless FLAC built from it) show a markedly lower
and more sharply-accelerating HF tail than the true-lossless/high-bitrate
files, and the spectrograms show it as a visible hard edge around 16 kHz
(clearest on `mp3_128.png`, where the chirp sweep line visibly terminates as
it crosses ~16 kHz) versus a clean line all the way to ~21 kHz on
`reference.png` and `mp3_320.png`.

## Phase 3: comparative cases

Twenty-one additional files, generated into a `comparative/` subdirectory to
keep them visually separate from the phase 1/2 single-file corpus. These
exist for the comparative (multi-file) analysis feature -- see
[comparative-quality-analysis.md](comparative-quality-analysis.md), whose
"Trust strategy" section is what this set is built to satisfy. Where phase
1/2 validate what a single-file heuristic can see, phase 3 validates the
**relationship classifier and ranking policy**: every file here exists as
half of a pair (or a link in a chain) whose ancestry, gain offset, time
offset, or master-identity is known by construction, so an engine's output
can be checked against a ground truth the generator itself defines rather
than a listening test. Generated by the same script; see `build_chain`,
`build_cross_codec_pair`, and `build_adversarial` in
`scripts/make-analysis-corpus.sh`. A third verification pass
(`verify_comparative`) writes `_verification_comparative.tsv` alongside the
phase 1/2 data files, with the measured numbers in the table below plus a
pair-level table recording the relationship class and expected ranking for
every pair (duplicated in prose here).

### Ordered degradation chains

The core ground truth for ranking. Built as a **successive** chain -- each
step decodes the *previous* step's lossy output and re-encodes it, rather
than three independent single-generation encodes of the same master. That
compounding is what makes the partial order unambiguous: each generation can
only lose more information than the one before it.

| File | Recipe | Ground truth |
| --- | --- | --- |
| `chain_master.wav` | Copy of `reference.wav` | The chain's root -- bit-identical to `reference.wav`. |
| `chain_320.mp3` | `lame -b 320` of `chain_master.wav` | First-generation lossy encode of the master. |
| `chain_192.mp3` | Decode `chain_320.mp3` -> `lame -b 192` | Second-generation: encodes the *already-lossy* 320 output, not the master. |
| `chain_128.mp3` | Decode `chain_192.mp3` -> `lame -b 128` | Third-generation, same compounding pattern. |
| `chain_128_rewrap.flac` | Decode `chain_128.mp3` -> `flac --best` | Lossless re-wrap of `chain_128.mp3` -- bit-identical audio, different container. |
| `real_chain_master.wav` | Copy of `real_reference.wav` | Same chain, real-music source. |
| `real_chain_320.mp3` / `real_chain_192.mp3` / `real_chain_128.mp3` / `real_chain_128_rewrap.flac` | Same recipe as above, applied to `real_chain_master.wav` | Same chain, real-music source. |

Ground truth partial order: `chain_master` > `chain_320` > `chain_192` >
`chain_128` = `chain_128_rewrap` (tied -- same audio, different container).
Same order for the `real_chain_*` set. Measured HF RMS above confirms this
monotonically (see measurements table below): each generation step reads
strictly lower than the one before it, and the FLAC rewrap matches its
source MP3 to five decimal places.

### Same-master, cross-codec pair

| File | Recipe | Ground truth |
| --- | --- | --- |
| `cross_codec_aac256.m4a` | `ffmpeg -c:a aac_at -b:a 256k` of `reference.wav` (single generation, not chained) | Same master as `cross_codec_mp3128.mp3`, near-transparent. |
| `cross_codec_mp3128.mp3` | `lame -b 128` of `reference.wav` (single generation, not chained) | Same master, audibly degraded. |

Ground truth: `cross_codec_aac256.m4a` ranks above `cross_codec_mp3128.mp3`
on fidelity, but the codecs differ -- the classifier has to reach that
ranking via each codec's own artifact signature (MP3's pre-echo and HF
cutoff vs. AAC's transform-coding character at a much higher bitrate), not
by comparing against a single shared cutoff frequency the way the same-codec
chain above can.

### Adversarial / must-abstain cases

| Files | Recipe | Ground truth |
| --- | --- | --- |
| `gain_only_a.wav` / `gain_only_b.wav` | a = copy of `reference.wav`; b = `reference.wav` at `volume=-6dB` | Same master, literally the *same encode* (uncompressed PCM, no re-encode at all), only gain differs. No quality difference -- must not be ranked. |
| `offset_pair_a.wav` / `offset_pair_b.wav` | a = copy of `reference.wav`; b = `reference.wav` through `adelay=137.4\|137.4` -- 137.4 ms is 6059.34 samples at 44.1 kHz, not a whole number | Same master, same encode, offset only. Sub-sample alignment must run before anything else works; once aligned, tied. |
| `different_master_a.wav` / `different_master_b.wav` | a = `reference.wav` through `volume=8dB` + `alimiter` (loudness-war style, same recipe as `loud.wav`); b = `reference.wav` through `bass=g=-4:f=100` + `treble=g=5:f=6000` (different EQ tilt, left dynamic) | Aligns confidently -- it's the same performance -- but the residual is broadband and comparable to the signal (EQ + dynamics differences, not codec artifacts). Must classify `differentMaster` and abstain from fidelity ranking. |
| `denoised_vinyl.wav` | `afftdn=nr=20:nf=-25` applied to the existing `real_vinyl_sim.wav` (phase 2) | Same master as `real_vinyl_sim.wav`; most of the added hiss/clicks/crackle/rumble is genuinely removed. An engine reading this as "clean-ish" relative to `real_vinyl_sim.wav` is arguably correct -- this case is deliberately debatable, per the phase 2 v2 tuning notes' own admission that a surgically de-noised rip reads clean. |
| `different_recording_a.wav` / `different_recording_b.wav` | a = first 15s of `real_reference.wav`; b = first 15s of `reference.wav` | Genuinely unrelated content (real music vs. the synthetic tone/chirp/noise bed) -- no alignment should be found; must not force a match. |

### Phase 3 measurements

From `_verification_comparative.tsv` (same measurements as the phase 1/2
tables: integrated LUFS and true peak from `loudnorm`'s first-pass summary,
HF RMS above 17 kHz from `highpass=f=17000,astats`).

| File | Integrated LUFS | True peak (dBTP) | HF RMS >17 kHz (dB) |
| --- | --- | --- | --- |
| chain_master.wav | -13.6 | -2.1 | -26.8 |
| chain_320.mp3 | -13.7 | -2.5 | -28.1 |
| chain_192.mp3 | -14.1 | -2.3 | -30.6 |
| chain_128.mp3 | -14.8 | -3.5 | -36.3 |
| chain_128_rewrap.flac | -14.8 | -3.5 | -36.3 |
| real_chain_master.wav | -12.3 | -0.0 | -53.3 |
| real_chain_320.mp3 | -12.3 | -0.0 | -53.7 |
| real_chain_192.mp3 | -12.6 | -0.0 | -55.0 |
| real_chain_128.mp3 | -13.1 | +0.1 | -56.7 |
| real_chain_128_rewrap.flac | -13.1 | +0.1 | -56.7 |
| cross_codec_aac256.m4a | -13.6 | -2.8 | -26.9 |
| cross_codec_mp3128.mp3 | -14.4 | -2.5 | -36.1 |
| gain_only_a.wav | -13.6 | -2.1 | -26.8 |
| gain_only_b.wav | -19.6 | -8.1 | -32.8 |
| offset_pair_a.wav | -13.6 | -2.1 | -26.8 |
| offset_pair_b.wav | -13.6 | -2.1 | -26.9 |
| different_master_a.wav | -5.6 | +2.8 | -18.9 |
| different_master_b.wav | -9.5 | +2.5 | -21.8 |
| denoised_vinyl.wav | -12.6 | +0.0 | -65.3 |
| different_recording_a.wav | -12.6 | -0.0 | -54.9 |
| different_recording_b.wav | -13.6 | -2.7 | -40.4 |

Notes on the numbers:
- The chain's HF RMS is strictly monotonic in both the synthetic and
  real-music sets (-26.8 -> -28.1 -> -30.6 -> -36.3 for `chain_*`; -53.3 ->
  -53.7 -> -55.0 -> -56.7 for `real_chain_*`) -- each successive-generation
  encode measurably loses more top end than the last, confirming the
  construction rather than assuming it.
- `chain_128.mp3` and `chain_128_rewrap.flac` match to five decimal places
  in the raw TSV (-36.34852**4** vs -36.34852**3**) -- the FLAC rewrap is a
  lossless copy of the MP3's decoded PCM, exactly as with the phase 1
  fake-lossless files.
- `gain_only_b.wav` sits exactly 6 dB below `gain_only_a.wav` in LUFS (-13.6
  vs -19.6) *and* in HF RMS (-26.8 vs -32.8) -- the HF number moving in
  lockstep with loudness, rather than changing shape, is the signature of a
  pure gain shift with no spectral difference.
- `offset_pair_a.wav` and `offset_pair_b.wav` differ by only ~0.02 dB in HF
  RMS despite the 137.4 ms shift -- the shift doesn't change the file's
  aggregate statistics at all (as expected; these measurements are
  time-blind), which is exactly why alignment has to happen before any
  useful comparison can run.
- `different_master_a.wav` (-5.6 LUFS / -18.9 dB HF) reproduces `loud.wav`'s
  numbers from the phase 1 table exactly, as expected since it's the same
  recipe. `different_master_b.wav` (-9.5 / -21.8) is a distinctly different
  signature -- confirming the pair looks like two real masters, not one
  master and a trivial gain change.
- `denoised_vinyl.wav` (-65.3 dB HF) sits 15 dB below `real_vinyl_sim.wav`'s
  own -50.1 dB (phase 2 table) -- `afftdn` removed a substantial amount of
  the added high-frequency noise energy, consistent with the "reads
  cleaner" ground truth.
- The `chain_*`/`real_chain_*`/`fake_lossless_*` FLAC re-wrap recipes decode
  an MP3 to WAV over a pipe before re-encoding with `flac`; as with the
  phase 1 fake-lossless files, this emits a benign `skipping unknown chunk
  'LIST'` + `unexpected EOF` warning from `flac` (it can't know the sample
  count in advance from a pipe) -- not an error, and the output is verified
  bit-for-bit downstream of the MP3 either way.

### Phase 3 relationship/ranking ground truth

The assertion a future `ComparativeAnalysisEngine` benchmark checks its
output against (also written machine-readably as the second table in
`_verification_comparative.tsv`):

| Pair | Relationship | Expected ranking |
| --- | --- | --- |
| `chain_master.wav` vs `chain_320.mp3` | sameMaster | `chain_master.wav` > `chain_320.mp3` |
| `chain_320.mp3` vs `chain_192.mp3` | sameMaster | `chain_320.mp3` > `chain_192.mp3` |
| `chain_192.mp3` vs `chain_128.mp3` | sameMaster | `chain_192.mp3` > `chain_128.mp3` |
| `chain_128.mp3` vs `chain_128_rewrap.flac` | identical | tied (lossless re-wrap of the same bits) |
| `real_chain_master.wav` vs `real_chain_320.mp3` | sameMaster | `real_chain_master.wav` > `real_chain_320.mp3` |
| `real_chain_320.mp3` vs `real_chain_192.mp3` | sameMaster | `real_chain_320.mp3` > `real_chain_192.mp3` |
| `real_chain_192.mp3` vs `real_chain_128.mp3` | sameMaster | `real_chain_192.mp3` > `real_chain_128.mp3` |
| `real_chain_128.mp3` vs `real_chain_128_rewrap.flac` | identical | tied (lossless re-wrap of the same bits) |
| `cross_codec_aac256.m4a` vs `cross_codec_mp3128.mp3` | sameMaster | `cross_codec_aac256.m4a` > `cross_codec_mp3128.mp3` |
| `gain_only_a.wav` vs `gain_only_b.wav` | identical | tied (gain-only, no quality difference) |
| `offset_pair_a.wav` vs `offset_pair_b.wav` | identical | tied once aligned (137.4 ms / 6059.34-sample non-integer offset) |
| `different_master_a.wav` vs `different_master_b.wav` | differentMaster | abstain (loudness-war vs. dynamic remaster is not a fidelity ranking) |
| `denoised_vinyl.wav` vs `real_vinyl_sim.wav` | sameMaster | `denoised_vinyl.wav` reads cleaner -- arguable-but-defensible, see note above |
| `different_recording_a.wav` vs `different_recording_b.wav` | differentRecording | no ranking (must fail to align) |

## Real-world fixtures: Quality Comparison Samples

Seven real-world pairs live at
`Private/Audio Samples/Quality Comparison Samples/<Artist>/` -- not
generated by the script, and absent from worktrees like the rest of
`Private/`. These check the relationship classifier against material the
synthetic corpus cannot simulate: real encoder implementations, real
mastering decisions, and, for two of the seven, a relationship the synthetic
corpus cannot produce at all (a different performance, and a genuine
labeled remaster).

Formats below were confirmed with `ffprobe`/`afinfo`. Bitrates are the audio
stream's own bit rate (`ffprobe -select_streams a:0`), not the container's
overall bit rate, which every file except the two `.flac`s inflates with an
embedded cover-art `mjpeg` stream.

| Pair | File A | File B | Expected relationship |
| --- | --- | --- | --- |
| Bloc Party -- "Like Eating Glass" | `01 Like Eating Glass.m4a`: AAC, 290 kbps, 262.0s | `01 Like Eating Glass.mp3`: MP3, ~209 kbps VBR, 262.0s | sameMaster (pending alignment confirmation) |
| Digable Planets -- "Dog It" | `03 Dog It.m4a`: AAC, 266 kbps, 260.7s | `03 Dog It.mp3`: MP3, 192 kbps CBR, 260.7s | sameMaster (pending alignment confirmation) |
| Elliott Smith -- "Can't Make a Sound" | `15 Can't Make a Sound.mp3`: MP3, 192 kbps CBR, 258.8s | `15 Can't Make a Sound.m4a`: AAC, 257 kbps, 258.8s | sameMaster (pending alignment confirmation) |
| James Blake -- "Limit to Your Love" | `01_Limit_to_Your_Love.m4a`: AAC, 265 kbps, 277.4s | `06 Limit to Your Love [Feist Cover].mp3`: MP3, 320 kbps CBR, 280.1s | **sameMaster** -- see the correction note below; these hold the same recording despite the filenames |
| James Taylor -- "You've Got a Friend" | `02 - You've Got A Friend.mp3`: MP3, 320 kbps CBR, 269.7s | `06.-You've Got A Friend.flac`: FLAC, 16-bit/44.1kHz lossless, 268.7s | sameMaster (pending alignment confirmation) -- the ~1.0s duration gap is larger than the other same-master pairs' (all under 0.1s); worth confirming by alignment that it's a lead-in/edit trim rather than a partial-content mismatch |
| Mos Def -- "Breakdown" | `07 Breakdown.mp3`: MP3, 192 kbps CBR, 182.0s | `07 Breakdown.m4a`: AAC, 286 kbps, 180.5s | sameMaster (pending alignment confirmation) -- ~1.5s duration gap, same caveat as James Taylor above |
| Whiskeytown -- "Jacksonville Skyline" | `03 - Jacksonville Skyline (2026 Remaster).flac`: FLAC, 16-bit/44.1kHz lossless, 180.8s (2026 Remaster) | `03 Jacksonville Skyline.mp3`: MP3, 192 kbps CBR, 181.0s (original) | **differentMaster** -- a labeled remaster vs. the original mix; expect confident alignment but must abstain from fidelity ranking (loudness-war-vs-dynamic-remaster, not a "which is better" question) |

Notes:
- Ranking ground truth for the sameMaster pairs -- which file in each pair
  is actually closer to the source master -- is deliberately **not**
  recorded here. Per the task this analysis was built for, that judgment
  comes from the user later (likely via the blind-confirmation loop the
  comparative-analysis plan describes), on exactly the pairs the synthetic
  corpus can't stand in for. This table only commits to format facts and the
  expected relationship *class*, both decidable from metadata, duration, and
  filename labeling alone.
- Whiskeytown's FLAC is the one file in this set whose relationship is
  stated outright rather than inferred -- "2026 Remaster" is in the filename
  itself.
- **Correction (2026-08-27): James Blake is not a different-recording case.**
  This manifest originally read the `[Feist Cover]` filename as marking a
  different artist's performance. It does not: James Blake's "Limit to Your
  Love" *is* the Feist cover, and both files hold the same recording. Measured
  waveform coherence is 0.985 across three probe windows, which cannot happen
  between different performances. The duration difference (277.4s vs 280.1s)
  is trailing silence, not a different take. Treat this pair as `sameMaster`.
  The lesson is worth keeping: filename inference is not ground truth, and
  this manifest should record only what was measured.

## File index

Full file list (see `_spectrograms/<name>.png` for each):

```
reference.wav                     real_reference.wav
true_lossless.flac
loud.wav                          real_loud.wav
quiet.wav
tilt_bassy.wav
tilt_bright.wav
muffled.wav                       real_muffled.wav
hiss.wav                          real_hiss.wav
mp3_128.mp3                       real_mp3_128.mp3
mp3_320.mp3
aac_96.m4a
aac_256.m4a
fake_lossless_mp3128.flac         real_fake_lossless_mp3128.flac
fake_lossless_aac128.flac
real_tape_sim.wav                 real_vinyl_sim.wav
mp3_192_modern.mp3
mp3_192_early.mp3
mp3_192_intensity.mp3
transient_reference.wav
transient_mp3_128.mp3             transient_mp3_320.mp3
transient_fake_lossless_mp3128.flac

comparative/chain_master.wav              comparative/real_chain_master.wav
comparative/chain_320.mp3                 comparative/real_chain_320.mp3
comparative/chain_192.mp3                 comparative/real_chain_192.mp3
comparative/chain_128.mp3                 comparative/real_chain_128.mp3
comparative/chain_128_rewrap.flac         comparative/real_chain_128_rewrap.flac
comparative/cross_codec_aac256.m4a
comparative/cross_codec_mp3128.mp3
comparative/gain_only_a.wav               comparative/gain_only_b.wav
comparative/offset_pair_a.wav             comparative/offset_pair_b.wav
comparative/different_master_a.wav        comparative/different_master_b.wav
comparative/denoised_vinyl.wav
comparative/different_recording_a.wav     comparative/different_recording_b.wav
```

50 audio files total: 29 from phase 1/2 (14 synthetic + 6 real-music v1,
9 phase-2 provenance cases) plus 21 phase-3 comparative cases, plus a
spectrogram PNG per phase 1/2/3 file and the three `_verification*.tsv` data
files used to build the tables above. (The seven real-world fixture pairs in
"Quality Comparison Samples" are separate, pre-existing files -- not
generated by this script -- documented above but not counted here.)

## Regenerating

```
scripts/make-analysis-corpus.sh                # generate anything missing
scripts/make-analysis-corpus.sh --force         # regenerate everything
scripts/make-analysis-corpus.sh /some/other/dir # write elsewhere
```

Full regeneration takes well under a minute on Apple Silicon; the verify +
spectrogram pass (which always re-runs) adds roughly 25 s.
